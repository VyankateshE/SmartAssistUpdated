const express = require("express");
const router = express.Router();
// const {
//   verifyToken,
// } = require("../middlewares/validators/verifytokenMiddleware");
// const requestLogger = require("../middlewares/fileLogs/requestLogger");
// const whatsApp = require("../utils/chatWhatsApp");

// router.use(verifyToken, requestLogger);

// router.post("/whatsapp-send-msg", whatsApp.chatWhatsApp);
// router.get("/whatsapp-redirect", whatsApp.redirectToWhatsApp);

module.exports = router;
